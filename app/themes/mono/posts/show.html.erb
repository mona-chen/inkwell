<article>
  <h1 class="text-2xl font-bold mb-2"># <%= @post.title %></h1>
  <p class="text-green-600 mb-8">
    <%= @post.author.name %> · <%= @post.published_at&.strftime("%Y-%m-%d") %>
  </p>
  <%= BlockRenderer.render(@post.content_blocks, self) %>
</article>
