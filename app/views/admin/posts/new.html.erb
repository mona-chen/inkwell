<%= form_with model: [:admin, @post] do |f| %>
  <div class="mb-6">
    <%= f.text_field :title, placeholder: "Post title", class: "w-full text-3xl font-bold border-0 focus:ring-0 px-0 placeholder:text-gray-300" %>
  </div>
  <%= render "admin/posts/editor", f: f, post: @post %>
<% end %>
